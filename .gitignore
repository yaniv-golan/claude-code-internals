CLAUDE.md

# Upstream submodule - cloned separately
ruvector/

# Claude Flow working files
.claude-flow/

# OS files
.DS_Store
.claude/
.mcp.json
.swarm/
AGENTS.md

# Internal research docs
docs/internal/

# Release build artifact (built by .github/workflows/release.yml; must NOT be tracked —
# a tracked copy makes `zip -r` APPEND stale files into every release asset)
/claude-code-internals.zip
